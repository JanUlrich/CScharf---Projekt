<Window x:Class="Auftragsmanagement_System.MainWindow.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindowView" Height="485" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="400*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <ContentControl Content="{Binding ActionBar}" Margin="0,0,0,0" Grid.Row="0"/>
        <ContentControl Content="{Binding Content, Mode=TwoWay}" Margin="10,0,0,0" Grid.Row="1"/>
        <Grid Grid.Row="2" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding ZeigeReporting}" Content="Report" Margin="10,0,10,5" Grid.Column="0"/>
            <Button Command="{Binding ZeigeMitarbeiterverwaltung}" Content="Mitarbeiter" Margin="10,0,10,5" Grid.Column="1"/>
            <Button Command="{Binding ZeigeArtikelverwaltung}" Content="Artikel" Margin="10,0,10,5" Grid.Column="2"/>
            <Button Command="{Binding ZeigeAuftragsverwaltung}" Content="Aufträge" Margin="10,0,10,5" Grid.Column="3"/>
        </Grid>
        </Grid>
</Window>
