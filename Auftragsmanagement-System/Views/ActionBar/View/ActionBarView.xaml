<UserControl x:Class="Auftragsmanagement_System.Views.ActionBar.View.ActionBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="50" Width="557">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Visibility="{Binding Command1Show}" Content="{Binding Command1Text}" Command="{Binding Command1}" Grid.Column="0" Margin="10"/>
        <Button Visibility="{Binding Command2Show}" Content="{Binding Command2Text}" Command="{Binding Command2}" Grid.Column="1" Margin="10"/>
        <Button Visibility="{Binding Command3Show}" Content="{Binding Command3Text}" Command="{Binding Command3}" Grid.Column="2" Margin="10"/>
        <Button Visibility="{Binding Command4Show}" Content="{Binding Command4Text}" Command="{Binding Command4}" Grid.Column="3" Margin="10"/>

    </Grid>
</UserControl>
