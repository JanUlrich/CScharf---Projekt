<UserControl x:Class="Auftragsmanagement_System.Views.Auftragsverwaltung.View.AuftragsverwaltungView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0,0,-418,-117">
        <DataGrid ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder, Mode=TwoWay}" HorizontalAlignment="Left" Width="282" Margin="0,0,0,0"/>
        <DataGrid ItemsSource="{Binding Path=SelectedOrder.OrderLines}" HorizontalAlignment="Left" Width="282" Margin="282,0,0,0"/>
        <Label Content="{Binding SelectedOrder.Order.Description}" HorizontalAlignment="Left" Margin="606,122,0,0" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
